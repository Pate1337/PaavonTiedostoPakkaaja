package tiedostopakkaaja;

import ui.Program;

import algorithm.Node;
import datastructures.PriorityQueue;

public class App 
{

    public static void main( String[] args ) {
        /*PriorityQueue<Node> p = new PriorityQueue<>((l, r) -> l.frequency - r.frequency);
        // Node n = new Node('a', 2);
        // p.add(n);
        // p.add(new Node('g', 6));
        p.insert(new Node('a', 10));
        p.insert(new Node('h', 1));
        p.insert(new Node('d', 5));
        p.insert(new Node('g', 13));
        p.insert(new Node('k', 12));
        p.insert(new Node('a', 4));
        System.out.println(p.get(0).frequency);
        System.out.println(p.get(1).frequency);
        System.out.println(p.get(2).frequency);
        System.out.println(p.get(3).frequency);
        System.out.println(p.get(4).frequency);
        System.out.println(p.get(5).frequency);
        p.compareValues(0, 1);*/
        Program.start();
    }
}
